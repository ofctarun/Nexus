# Production environment — Deployed on Vercel
# In production, the client uses relative paths (/api) which are proxied
# by Vercel's rewrite rule to https://nexus-h82b.onrender.com
# No VITE_API_URL needed — defaults to '/api' and Vercel handles routing
